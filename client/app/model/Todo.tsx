export type TodoType = {
    id: number;
    title: string;
    isCompleted: boolean;
}

export type InsertTodoType = {
    title: string;
}

export type UpdateTodoType = {
    isCompleted: boolean;
}

